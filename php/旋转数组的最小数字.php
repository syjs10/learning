<?php

function minNumberInRotateArray($rotateArray)
{
      $startIndex = 0;
      $endIndex   = count($rotateArray) - 1;
      $midIndex   = round((startIndex + endIndex + 1) / 2);

      $if(condition) {
            # code...
      }
}

var_dump(minNumberInRotateArray(array(3, 4, 5, 1, 2)));
